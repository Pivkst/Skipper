<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="colorText">#000000</item>
        <item name="colorTextHint">#cccccc</item>
        <item name="colorBackground">#ffffff</item>
        <item name="colorBackgroundSecondary">#bbbbbb</item>
        <item name="nextTheme">Dark Theme</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="DarkTheme" parent="AppTheme">

        <item name="colorText">#ffaa00</item>
        <item name="colorTextHint">#885500</item>
        <item name="colorBackground">#111111</item>
        <item name="colorBackgroundSecondary">#555555</item>
        <item name="nextTheme">Light Theme</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
